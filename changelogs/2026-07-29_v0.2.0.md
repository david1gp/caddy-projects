## [0.2.0] - 2026-07-29

### 🚀 Features

- Initial caddy-projects api, cli and caddy config generation
- Port collision checks, auto-assign, delete-by-port, rename apply to regenerate
- *(cli)* Rewrite with @stricli/core, validation, and boolean enable/disable flags
- *(config)* Summary table, select by name/domain/port, config all for full JSON
- *(config)* Support custom static routing and path-scoped OIDC

### 🐛 Bug Fixes

- *(oidc)* Require 32 or 64 byte cookie secret
- Docs without path skips docs routes instead of failing
- *(socket)* Use shared runtime directory for CLI connections

### 🏭 Operations

- Add caddy-oidc install and Zitadel OIDC provisioning scripts
- *(release)* Switch to non-interactive release script

### 📚 Documentation

- Add why/how and api reference; fix daemon skip flags
